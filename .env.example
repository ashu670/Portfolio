GITHUB_USERNAME=ashu670

# REQUIRED for full contribution heatmap (uses GitHub GraphQL API)
# Without this, the heatmap will only show public push events (very limited)
# Get token at: https://github.com/settings/tokens
# Scopes needed: read:user
GITHUB_TOKEN=your_github_personal_access_token_here

PORT=5000
